/**
 * _abs - print the absolute value of n.
 * @n: the number to computes the abs value.
 * Return: the absolute value of an integer n.
 */
int _abs(int n)
{
	if (n >= 0)
	}
		return (n);
	}
	else
		return (-n)

}
