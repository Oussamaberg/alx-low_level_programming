#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry piont
 * generat a random int var n
 * Return: positive if the n is > 0 or negative if n < 0
 */
int main(void)
{
	int i;
	int j;
	int l;
	int m;

	for (i = 48; i < 58; i++)
	{
		for (j = 0; j < 98; j++)
		{
			putchar(j + '0');
		}
	}

	return (0);
}
