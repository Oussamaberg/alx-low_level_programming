/**
 * _isupper - checks if c is uppercase.
 * @c: The character to check
 *
 * Return: 1 if c is uppercase 0 if it's not.
 */
int _isupper(int c)
{
	if (c >= 'A' && <= 'Z')
		return (1)
	else
		return (0)
}
